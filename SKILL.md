---
name: ai-matrix-product-workflow
description: 为固德威 AI MATRIX 项目执行产品归档、PRD、HTML 原型和 Figma 原型交付。适用于 AI运维平台、AI数据中台、AI服务中台、数智管理基座及跨平台全局规则；默认不用于技术方案、研发、测试或发布工作。
---

# AI MATRIX 产品交付工作流

## 目标

把需求从范围确认推进到可追踪 PRD、可评审 HTML 原型和可编辑 Figma 原型，同时保持产品边界、文件归属和阶段基线一致。

## 开始前

1. 查找并完整读取当前工作区的 `AGENTS.md`、项目说明和同目录规则文件。
2. 以用户最新指令和当前项目文件为事实源。本 Skill 只提供便携基线；项目内更新规则优先。
3. 判断目标、范围、对象、角色、用途和关键口径是否明确。关键歧义会改变产物时，先提问，不基于猜测生成正式 PRD 或原型。
4. 新建、移动或删除文件前，说明目标路径、影响范围和预期结果并获得确认；针对现有文件的局部批注修改可直接执行。

## 按任务读取参考资料

- 涉及平台归属、目录选择、文件命名或跨功能需求池时，读取 [产品边界与归档规则](references/产品边界与归档规则.md)。
- 涉及 PRD、HTML 原型、阶段确认或变更回退时，读取 [PRD与原型阶段门禁](references/PRD与原型阶段门禁.md)。
- 涉及页面设计、Goodwe UI、Figma 写入或原型验收时，读取 [Goodwe UI与Figma交付规范](references/Goodwe_UI与Figma交付规范.md)。

只读取当前任务需要的参考文件，不默认加载全部资料。

## 默认交付流程

```text
范围与口径确认
  -> Requirements / PRD Draft
  -> 用户确认 PRD，建立 Requirements Baseline
  -> HTML 原型 Draft
  -> 用户确认 HTML，建立 UI Design Baseline
  -> 使用 Figma 官方 MCP 写入已确认的目标文件
  -> Figma 截图与节点结构回读
  -> 原型交付
```

硬门禁：

- PRD 未确认，不进入 HTML 原型。
- HTML 未确认，不写入 Figma。
- 业务范围、角色、流程或规则发生实质变化时，先回退更新 PRD，再同步 HTML。
- 仅布局、视觉或交互表达变化时，更新 HTML 并重新确认，不擅自改变业务能力。

## 默认边界

- 默认交付：PRD、可交互 HTML 原型、经确认后写入的 Figma 原型和三者之间的追踪说明。
- 默认不交付：技术 Design、研发 Tasks、生产代码、数据库迁移、测试用例、系统测试、发布记录和部署实施。
- 交付物的内容、结构、视觉与交互回读属于产品自检，不等同于研发测试。
- 用户明确扩大范围后，才进入技术、研发、测试或发布工作。

## 工具路由

- 创建或修订正式 PRD 时，可使用环境中可用的 PRD Skill，但必须保留本工作流的边界、编号和确认门禁。
- 创建或修改原型时，坚持 HTML-first；HTML 是 Figma 写入前的结构与视觉事实源。
- Figma 阶段必须使用 Figma 官方 MCP，以及环境中可用的 `figma` 和原型工作流 Skill。不要使用浏览器点击、剪贴板导入或整页截图代替原生可编辑结构。

## 完成判定

交付前确认：

- 文件归属、名称和版本符合当前项目规则。
- PRD、HTML 和 Figma 中的页面、需求编号、状态和关键交互能够相互追踪。
- 未确认内容明确标注为“待确认”，没有被包装成既定事实。
- HTML 可打开，核心交互可评审，目标视口没有重叠或裁切。
- Figma 写入完成后已经回读根 Frame 截图、节点结构、尺寸、关键文案和页面名称，并提供带节点标识的链接。
